{
  "name": "whatsappme-mcp",
  "version": "1.0.0",
  "description": "MCP server that lets Claude message you on WhatsApp",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "prestart": "bun install --silent",
    "start": "bun run src/index.ts",
    "dev": "bun --watch src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "@ngrok/ngrok": "^1.4.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "bun-types": "latest"
  }
}
